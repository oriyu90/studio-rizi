# Studio RIZI UI・共有情報監査（2026-08-27）

## 範囲

Studio RIZI本体のHOME / NEWS / PROFILE × 日本語・英語・中文・ポルトガル語、共通ヘッダー／フッター／プロジェクト詳細、404ページ。独立した `website/projects/` の製品サイトとKiziの実装は変更していない。公開先は既存の `https://studio-rizi.pages.dev/`。

## 監査で確認した問題と変更

| 問題 | 対応 |
| --- | --- |
| OSダーク＋`?theme=light`でダーク用の文字色や縁取りが残る。プロフィール本文と非選択ニュースフィルターが白背景上で薄い | 無条件の旧ダーク指定を撤去。`theme.css`の共通色に統合し、`theme.js`がOS／明示指定と画像を同期 |
| 明示ダーク指定がなく、ライト指定時の画像変更も片方向 | ライト・ダークの両指定、OS変更追従、旧MediaQueryList APIのフォールバック |
| カードが非正方形。390×844では346×約212pxの1列 | `aspect-ratio:1`、縦横比による多列配置。390×844では約172.5px四方の2列、初期3行 |
| 指定と異なる2ボタンの表示切替 | 次の表示方式のアイコンだけを示す1ボタン。翻訳したaria-labelとtitle、46pxの操作領域 |
| 補助文字9px、320pxで言語メニュー消失 | 主要本文14px以上、補助11〜12px以上、言語メニュー常時表示。名称14px以上、詳細はリスト／ダイアログで全文表示 |
| 固定高さの案内カードで560px付近の英語に重なり | 560px以上の3枚横並びを維持し、内容に応じて高さが伸びる構成 |
| 短い画面で長いダイアログが圧迫 | dvh／vh上限、内部スクロール、追従する閉じるボタン、見出し折り返し |
| 緑の詳細アイコンなどで暗色テーマ時に文字のコントラストが不足 | アクセント背景は常に黒文字。青を`#3c51d6`へ調整し白文字とのコントラストを確保 |
| SNS画像説明が全言語で日本語、MIMEとXアカウント情報が未指定 | 4言語の画像説明、`og:image:type=image/png`、`twitter:site=@InovateofRIZI`を静的生成 |
| NEWSのLATESTが2026.08.19のまま（データ最新は08.24） | ビルド時にニュース全件の最新日付から生成。フィルター選択には依存させない |

追加修正後は小型カードでも説明文（最大3行）・番号・丸いアイコン・OPENを表示する。上段にアイコン／番号／OPEN、中央に名称と説明、下段に対応OSを配置し、非表示による密度調整をしない。説明全文はリスト／詳細で提供する。カードの検索条件・展開状態は表示切替でも保持する。

## テーマと寸法の設計

- CSS読込順は `styles.css` → `responsive.css` → `theme.css`。テーマ色は最後のファイルを一次情報とする。
- `--ink`は黄緑などアクセント背景上の暗い文字、`--text`はテーマに追従する本文。両者を混同しない。
- 通常はOS設定に追従。`?theme=light` / `?theme=dark`はそのページの確認用指定。指定なしではpictureの標準mediaクエリを維持する。
- JSなしではテーマのOSメディアクエリと静的カードリンクが利用可能。コンテナ単位未対応時にも固定の読みやすい文字サイズを残す。
- カード密度係数は0.68〜1。CSS Gridの最小辺長へ適用し、列数を増やして圧迫感を下げる。列数が変わる際の段階変化は意図したもの。
- 初期行数は幅／高さが0.75以下で3行、1.15以下で2行、それより横長なら1行。画面幅599px以下では2列を維持する。
- 余白・文字・画像領域に上下限を設け、縦長画面を単純な全体縮小では扱わない。

## SNSの確認範囲と仕様上の注意

- [Open Graph公式仕様](https://ogp.me/)の基本項目（title/type/image/url）、画像の寸法・形式・内容を説明するalt、locale、descriptionを確認。
- HOME / NEWS / PROFILEの題名・紹介文はページ別、4言語別。画像は共通のブランドビジュアルで、現在のスローガンと一致するため継続使用。英語版などの画像内文字は日本語のままであり、翻訳したaltで意味を説明する。
- PNG実寸1730×909、約1.5MB、HTMLの寸法と一致。公開画像URLはTwitterbot User-AgentでHTTP 200 / image/pngを確認。robots.txtはクロールを許可。画像は認証不要。
- `twitter:card=summary_large_image`、題名・説明・画像・altをHTMLに保持。JS実行を前提としない。
- [Xの従来の公式カード仕様URL](https://developer.x.com/en/docs/x-for-websites/cards/overview/summary-card-with-large-image)は監査時に一般的な`docs.x.com/overview`へ転送され、カードの数値仕様を再確認できなかった。Meta公式共有ベストプラクティスも取得制限（429）。第三者の相反する「2026年最新」サイズ記事を根拠に、画像を作り直したり互換タグを削除したりしていない。
- X/Meta等の実際の投稿表示、プラットフォームのキャッシュ更新は未検証。「すべてのSNSの最新仕様に完全準拠」とは断定しない。実投稿・外部キャッシュの強制更新は実施していない。

## 検証

- `npm test`: 27件成功。公開ファイル148件、完全重複0件、言語別HTML・canonical・hreflang・サイトマップ・内部参照の検査成功。
- Chromium系実ブラウザ: HOMEカード4言語×2テーマ×12寸法（96条件）、リスト4言語×2テーマ×4寸法（32条件）、NEWS/PROFILE 4言語×2テーマ×5寸法（80条件）を検査。発見した案内カードの重なりを修正・再確認。
- カード寸法: 320×568で141.5px四方／2列、390×844で172.5px四方／2列、768×1024で約233.8px四方／3列。900px幅では横長900×600で約274.1px／3列、縦長900×1600で約205.3px／4列。
- 検査した画面: 320×568、390×844、560×1000、768×1024、900×1600、900×600、1080×1920、1200×800、1440×900、1920×1080、2560×1080、844×390。
- 名称・対応OS・アイコンがカード領域内に収まること、主要テキストの横はみ出し、案内番号と見出しの重なり、単一アイコンの実表示を確認。
- [W3Cのリフロー説明](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html)を参考に320 CSS pxで検査。ブラウザの実ズーム／文字だけの拡大試験とは区別する。
- [W3Cのコントラスト説明](https://www.w3.org/WAI/WCAG22/Understanding/contrast-minimum.html)に基づく主要色の計算値: 黒／黄緑14.92:1、白／青6.29:1、ライト補助文字／白7.32:1、ダーク補助文字／暗背景10.95:1。写真上の文字を含むサイト全体のWCAG認証ではない。
- Safari・Firefox・DuckDuckGoの実機、SNSの投稿画面は未検証。標準Grid／aspect-ratioとコンテナ単位・dvhのフォールバックを用意している。

## 保守

言語生成先を直接編集しない。変更は日本語HTML、共通JS/CSS、SEO生成スクリプトへ行い `npm test` を実行する。CSS・JSのキャッシュ版は`20260827d`。GitHub mainへのpushから既存のPages自動デプロイを利用する。

## 同日追補: カード説明文・装飾の復元

- ユーザーの追加指示により、小型カードで説明文・番号・OPENを隠す仕様を撤回。正方形・多列配置は維持し、4段のGridで全要素を再配置した。
- 名称14px以上、説明12px以上、番号・OPEN・対応OS11px以上。説明は最大3行、全文は引き続きリスト／詳細で読める。丸いアイコンは最小24pxとし、MM/WKなど幅のある英字も枠内へ収める。
- Chromiumで4言語×2テーマ×15寸法のカード120条件を検査。小型アイコンのはみ出しを修正し、320/360/390pxの24条件を再確認。リスト表示も32条件で回帰確認した。
- 非表示設定の再導入を検出するテストを追加し、`npm test`は28件成功。ブラウザ検証では説明文と装飾の実表示、正方形、要素同士の重なり、領域外へのはみ出しを確認した。
