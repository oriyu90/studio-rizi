{
  "name": "studio-rizi-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate:seo": "node scripts/generate-seo-pages.mjs",
    "build": "npm run generate:seo && npm run validate && npm run count-files",
    "validate": "node scripts/validate-site.mjs",
    "count-files": "node scripts/count-files.mjs",
    "test": "npm run build && node --test scripts/*.test.mjs",
    "indexnow": "node scripts/submit-indexnow.mjs --all"
  }
}
